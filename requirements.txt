# ── Core API server ───────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Environment & config ──────────────────────────────────────
python-dotenv==1.0.1

# ── AI brain ─────────────────────────────────────────────────
anthropic==0.28.0

# ── System monitoring (Proactive Engine) ─────────────────────
psutil==5.9.8

# ── Networking ────────────────────────────────────────────────
websockets==12.0
httpx==0.27.0          # used by test_brain.py + anthropic SDK internally

# ── Redis (conversation memory — optional, graceful fallback) ─
redis==5.0.8

# ── [선택] Google Gemini — 추후 활성화 시 주석 해제 ───────────
# google-generativeai==0.7.2
